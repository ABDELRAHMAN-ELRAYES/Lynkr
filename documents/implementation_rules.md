## 1. Create the models of each module in prisma schema and migrate them seperately from the schema diagram 
## 2. Follow up the file structure from the readme files 
## 3. Don't use enum types in prisma schema
## 4. Modify and reuse the existing code (avoid duplication
)